.album[id="#{dom_id(album)}"]
  h2.title
    = link_to album.title, album.affiliated_url
    '
    'from
    = link_to album.site_title, album.affiliated_site_url
  .models
    span
      'Starring:
      '
      strong
        - album.site_models.each do |site_model|
          = link_to site_model.nickname, model_albums_path(site_model.model)
      ', added on
      strong = album.date
  .image
    = link_to album.affiliated_url do
      = image_tag album.cover_url, class: 'img-fluid img-thumbnail mx-auto d-block'
